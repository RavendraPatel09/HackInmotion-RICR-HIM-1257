# Database configuration (defaults to SQLite fallback if not set)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/nagarsathi

# JWT Authentication Configuration
JWT_SECRET_KEY=f604b77f985047b864d436bf64c207cd454b5dfd4f647970d4d12f1712a67e2a
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# SMTP configuration for real OTP email notifications
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM_EMAIL=noreply@nagarsathi.gov.in
SMTP_FROM_NAME=NagarSathi

# Allowed CORS origins (JSON array string format)
CORS_ORIGINS=["http://localhost:5173", "http://localhost:3000"]
